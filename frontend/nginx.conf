server {
  listen 80;
  root /usr/share/nginx/html;

  location /api/ {
    proxy_pass http://backend:8080;
    proxy_set_header Host $host;
  }

  location / {
    # react-router-dom 7: qualquer rota cai no index e o roteador resolve
    try_files $uri $uri/ /index.html;
  }
}
